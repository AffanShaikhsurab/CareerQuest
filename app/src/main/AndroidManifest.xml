<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.affanshaikhsurab.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".activity.aptitude"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ChatBot"
            android:exported="false"></activity>
        <activity
            android:name=".activity.collage_Information"
            android:exported="false"
            android:label="@string/title_activity_collage_information"
            android:theme="@style/Theme.MyApplication" /> <!-- <activity -->
        <!-- android:name="act" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".activity.coursesInformation"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".carrersList.collageList" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".activity.Information"
            android:exported="true"
            android:label="HELLO" />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
        </activity>
    </application>

</manifest>